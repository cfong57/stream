<div class="row span8">
  <h1>All Users</h1>
  <p>
    Index of all users visible to you.</br>
    <%= sortable "name", "Sort A-Z" %> ● 
    <%= sortable "created_at", "Sort by join date" %>
    <% #=sortable "uploads", "Sort by number of uploads"    need some clever hack for this one %> 
  </p>
  <%= render @users %>
  <%= will_paginate @users %>
</div>